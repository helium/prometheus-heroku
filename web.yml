basic_auth_users:
    $PROMETHEUS_USERNAME: $PROMETHEUS_ENCRYPTED_PASSWORD

tls_server_config:
    cert_file: "./helium-certs/console-prometheus.helium.wtf.crt"
    key_file: "./helium-certs/console-prometheus.helium.wtf.key"
