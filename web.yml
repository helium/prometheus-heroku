basic_auth_users:
    $PROMETHEUS_USERNAME: $PROMETHEUS_ENCRYPTED_PASSWORD

# tls_server_config:
    # client_auth_type: "RequireAndVerifyClientCert"
    # client_ca_file: "../etc/ssl/certs/ca-certificates.crt"
