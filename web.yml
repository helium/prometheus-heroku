basic_auth_users:
    $PROMETHEUS_USERNAME: $PROMETHEUS_ENCRYPTED_PASSWORD

tls_server_config:
    cert_file: "./certs/console_prometheus_helium_wtf-injected.crt"
    key_file: "./certs/console_prometheus_helium_wtf-injected.key"
